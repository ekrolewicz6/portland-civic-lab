# Data table provenance notes

Every CSV in this directory carries `fy, basis, doc_id, page, value_raw, notes`
columns. `basis` ranks: actual-acfr > actual-budgetary > revised > adopted >
approved > proposed. Function/object dimensions use ODE PBAM codes (lib/coa.ts),
never raw labels.

Per-table caveats accumulate here as extraction proceeds.

- (extraction not yet started)
