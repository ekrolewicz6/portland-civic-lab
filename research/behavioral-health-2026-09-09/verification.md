# Verification record — September 9, 2026

The user story is reading the Council evidence in either deep dive, inspecting its qualifications, comparing appropriate clinical settings, exploring the psychiatric handoff, and preparing an aggregate-records request without sending it. These additions use static typed records and local interactions; they add no API or database dependency.

## Checks completed

| Check | Result |
|---|---|
| Application TypeScript | Passed `npx tsc --noEmit --incremental false`. |
| Ingest TypeScript | Passed `npx tsc --noEmit -p ingest/tsconfig.json`; no ingest changes. |
| Project lint | Passed `npx eslint . --ignore-pattern '.claude/**'`. The unfiltered repository command reports 13 errors in generated `.next` JavaScript inside an unrelated existing `.claude/worktrees/` checkout. No lint configuration was changed to hide those files. |
| Production build | Passed in `/private/tmp/civic-bh-20260909-build`, with the same source/configuration/dependencies, excluding local environment credentials and unrelated worktrees. Network access was needed for existing Google Fonts. The app reports its expected missing-database warning for unrelated dashboard routes; these two deep dives render statically. |
| Focused browser tests | All four tests in `e2e/behavioral-health.spec.ts` passed: desktop 1440×1000 and mobile 390×844. |
| Keyboard and clipboard | Enter opens evidence disclosures and selects the psychiatric journey; Space selects requests; every new request copies successfully with explicit not-sent status. Existing analytics is blocked in local tests. No application write request is generated by the copy workflow. |
| Navigation and rendering | Both route transitions work; all same-page fragment targets resolve; source PDFs return HTTP 200/application-pdf. No page errors or horizontal overflow in either viewport, including expanded evidence tables. |
| Visual inspection | Inspected six rendered section screenshots covering evidence, care definitions and psychiatric journey at desktop/mobile widths. Text, bars, disclosures and route controls remain readable and usable. |
| Source files | Both citation PDFs match the original Downloads attachments byte-for-byte; SHA-256 hashes are in `sources.md`. |
| External citations | The new public URLs were checked. A removed County register (404) was replaced by the original action plan carrying the same assignment and deadline. CCC returned 403 to a plain HTTP client but its page and criteria were confirmed through web retrieval. Other sources returned 200. |
| Diff hygiene | `git diff --check` passed. Existing unrelated untracked `research/continuum-review-2026-09-08/HANDOFF-AND-NEXT-STEPS.md` was preserved. |

## Evidence boundaries

The tests cover the revised public pages and copy-only request workflow. The database-dependent full smoke suite was not run against a seeded database. No migration, production data change, deployment or outgoing records request was performed.

Source verification does not reconcile the unresolved cohort estimates, establish Council adoption, or validate a psychiatric simulation. Those remain explicitly open research questions and are reflected in the draft requests.

Local visual artifacts were captured under `/private/tmp/civic-bh-{homelessness,continuum,journey}-{1440,390}.png`; browser diagnostics are in `/private/tmp/civic-bh-visual-check.json`. The final isolated build log is `/private/tmp/civic-bh-build-final.log`.
