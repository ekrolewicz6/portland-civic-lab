{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push *)",
            "command": "cd /Users/edankrolewicz/Code/Active/portland/dashboard && set -a && source .env.local && set +a && npx tsx scripts/verify-api-queries.ts 2>&1",
            "timeout": 60,
            "statusMessage": "Verifying API queries before push..."
          }
        ]
      }
    ]
  }
}
